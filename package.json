{
  "name": "@alsania-io/devcon-x",
  "version": "1.0.0",
  "private": true,
  "description": "Root package for DevConX monorepo",
  "repository": "https://github.com/alsania-dev/devconx",
  "license": "Apache-2.0",
  "type": "module",
  "workspaces": [
    "extensions/vscode"
  ],
  "scripts": {
    "build": "cd extensions/vscode && npm run build",
    "lint": "cd extensions/vscode && npm run lint",
    "test": "node --test \"tests/**/*.test.js\"",
    "check": "npm run lint && npm run build && npm test",
    "pack-extension": "cd extensions/vscode && vsce package"
  },
  "devDependencies": {
    "@vscode/vsce": "^3.9.2"
  },
  "dependencies": {
    "@genkit-ai/mcp": "^1.39.0"
  }
}
